name: AI Services - Validación Comparativa de Modelos

on:
  workflow_dispatch:
  push:
    paths:
      - "ai-services/**"
      - ".github/workflows/ai-ml-model-comparison.yml"

jobs:
  compare-models:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai-services
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('ai-services/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          if [ -f requirements-test.txt ]; then pip install -r requirements-test.txt; fi

      - name: Run model comparison (v2)
        run: |
          ls -la
          python validate_models_comparison_v2.py --dataset augmented_dataset_full_20251103_124126.csv --test-size 3000

      - name: Collect comparison artifacts
        run: |
          mkdir -p ../artifacts
          cp model_comparison_*.txt ../artifacts/ || true
          cp model_comparison_*.md ../artifacts/ || true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ai-model-comparison
          path: artifacts


